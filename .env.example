# vmhub environment — variable NAMES only. Copy to .env and fill values.
# .env is gitignored. Never commit real values.

# --- Proxmox (runtime credential: scoped API token, not root password) ---
PVE_HOST=
PVE_TOKEN_ID=vmhub@pve!automation
PVE_TOKEN=

# --- iLO (provision-only; used once to install Proxmox + bootstrap) ---
ILO_HOST=
ILO_USERNAME=
ILO_PASSWORD=

# --- GitHub App (per-lease installation tokens for VM repo access) ---
GH_APP_ID=
GH_APP_INSTALLATION_ID=
GH_APP_PRIVATE_KEY=
GH_ALLOWED_REPOS=

# --- Optional ---
VMHUB_LEASE_DIR=./leases
VMHUB_ARTIFACT_DIR=./artifacts
VMHUB_DISK_FULL_REFUSAL_PCT=15
